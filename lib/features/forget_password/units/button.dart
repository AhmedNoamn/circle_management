part of '../view.dart';

class _ButtonUnit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return CustomButton(
      onTap: () {},
      child: Text(
        'Reset Password',
        style: style2,
      ),
    );
  }
}
